import { PricingConfig } from '@/types/pricing'

export const pricingConfig: PricingConfig = {
  additionalPrices: {
    customized: 99,
    'full-service': 299
  },
  supportDuration: {
    template: 30,
    customized: 60,
    'full-service': 90
  }
}